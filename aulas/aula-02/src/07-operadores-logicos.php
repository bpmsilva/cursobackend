<?php
    // Operadores de comparação
    $igual = 10 == 5; // Igualdade
    $identico = 10 === 5; // Identidade
    $diferente = 10 != 5; // Diferença
    $maior = 10 > 5; // Maior que
    $menor = 10 < 5; // Menor que
    $maior_igual = 10 >= 5; // Maior ou igual
    $menor_igual = 10 <= 5; // Menor ou igual

    // Operadores lógicos
    $e = true && false; // E
    $ou = true || false; // OU
    $nao = !true; // NÃO
?>